# Fichier de configuration pip pour éviter les problèmes de déploiement
[global]
no-cache-dir = true
disable-pip-version-check = true

# Éviter les installations automatiques de modèles
no-binary = spacy
prefer-binary = beautifulsoup4,lxml,numpy,pandas