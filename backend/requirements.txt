# --- Core API ---
fastapi==0.104.1
uvicorn[standard]==0.24.0
pymongo==4.6.0
python-multipart==0.0.6
aiofiles==23.2.1
python-dotenv==1.0.0
argon2-cffi==23.1.0
orjson>=3.10

# --- Authentification & Sécurité ---
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
certifi>=2024.2.2

# --- OpenAI (client HTTP uniquement, pas de modèles locaux) ---
openai==1.58.0

# --- Analyse de sentiment (léger, local) ---
vaderSentiment==3.3.2
textblob==0.17.1

# --- Services viraux avancés ---
redis==5.0.1

# --- Flux RSS / scraping HTML ---
feedparser==6.0.10
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3
sgmllib3k==1.0.0

# --- API Reddit (optionnel) ---
praw==7.7.1

# --- Requêtes HTTP async ---
httpx==0.25.2
aiohttp==3.9.1

# --- Streaming audio léger ---
streamlink==6.5.1
ffmpeg-python==0.2.0

# --- Scheduling ---
APScheduler==3.10.4

# --- Résumé automatique ---
sumy==0.11.0

# --- Data utils ---
pandas==2.1.4
numpy==1.26.4
python-dateutil==2.8.2

# --- Scraping réseaux (stabilité prod) ---
snscrape==0.7.0.20230622

# --- Génération PDF / images ---
reportlab==4.0.7
pillow==10.1.0

# --- Rendu serveur du bilan PNG (navigateur headless) ---
# Nécessite aussi le navigateur : `python -m playwright install chromium`
# (ajouté en buildCommand, non bloquant). Si indisponible, le bilan hebdo
# retombe automatiquement sur le digest texte.
playwright==1.49.0

# --- Telegram (léger) ---
python-telegram-bot==20.7

# --- Web Push notifications ---
pywebpush==2.0.0
py-vapid==1.9.1

# --- Prod (optionnel) ---
gunicorn==21.2.0

# --- DNS ---
dnspython>=2.6

# --- Tagging local (normalisation accents) ---
Unidecode==1.3.8

# --- Analyse de texte avancée ---
nltk==3.8.1

# --- Gestion des erreurs et monitoring ---
sentry-sdk[fastapi]==1.40.0